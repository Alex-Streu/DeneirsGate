@using DeneirsGate.Services
@using CustomHtmlHelpers
@model List<TrapViewModel>

<div class="flexbox-container full-width">
    @HtmlHelpers.RenderFancyTextbox("lg long", "SearchTraps", null, "", "Search")
</div>

<div id="trap-inventory" class="flexbox-container justify-center">
    @foreach (var item in Model)
    {
        <div id="@item.TrapKey" class="btn btn-lg btn-default menu-option menu-link" data-url="@Url.Action("_Trap", "ContentSearch", new { id = item.TrapKey })" data-dir="@item.Name">
            @item.Name
        </div>
    }
</div>

<script type="text/javascript">
    $('#SearchTraps').keyup(function () {
        var search = $(this).val().toLowerCase();
        $('#trap-inventory .menu-link').each(function () {
            var name = $(this).text().toLowerCase();
            if (name.indexOf(search) > -1) {
                $(this).show();
            }
            else {
                $(this).hide();
            }
        })
    })
</script>
