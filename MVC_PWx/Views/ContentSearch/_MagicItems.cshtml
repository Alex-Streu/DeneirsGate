@using DeneirsGate.Services
@using CustomHtmlHelpers
@model List<MagicItemViewModel>

<div class="flexbox-container full-width">
    @HtmlHelpers.RenderFancyTextbox("lg long", "SearchItems", null, "", "Search")
</div>

<div id="item-inventory" class="flexbox-container justify-center">
    @foreach (var item in Model)
    {
        <div id="@item.ItemKey" class="btn btn-lg btn-default menu-option menu-link" data-url="@Url.Action("_MagicItem", "ContentSearch", new { id = item.ItemKey })" data-dir="@item.Name">
            @item.Name
        </div>
    }
</div>

<script type="text/javascript">
    $('#SearchItems').keyup(function () {
        var search = $(this).val().toLowerCase();
        $('#item-inventory .menu-link').each(function () {
            var name = $(this).text().toLowerCase();
            if (name.indexOf(search) > -1) {
                $(this).show();
            }
            else {
                $(this).hide();
            }
        })
    })
</script>