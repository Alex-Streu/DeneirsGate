@using DeneirsGate.Services
@using CustomHtmlHelpers
@model List<SettlementViewModel>

<div class="flexbox-container full-width">
    @HtmlHelpers.RenderFancyTextbox("lg long", "SearchSettlements", null, "", "Search")
</div>

<div id="settlement-inventory" class="flexbox-container justify-center">
    @foreach (var item in Model)
    {
        <div id="@item.SettlementKey" class="btn btn-lg btn-default menu-option menu-link" data-url="@Url.Action("_Settlement", "ContentSearch", new { id = item.SettlementKey })" data-dir="@item.Name">
            @item.Name
        </div>
    }
</div>

<script type="text/javascript">
    $('#SearchSettlements').keyup(function () {
        var search = $(this).val().toLowerCase();
        $('#settlement-inventory .menu-link').each(function () {
            var name = $(this).text().toLowerCase();
            if (name.indexOf(search) > -1) {
                $(this).show();
            }
            else {
                $(this).hide();
            }
        })
    })
</script>